# Environment
NODE_ENV=development

# Server
SERVER_PORT=4000

# Legacy compatibility (temporary)
# PORT=4000

# MongoDB
# MONGO_URI=mongodb+srv://oggainzofficial:balaji.admin@cluster0.bwfvssh.mongodb.net/oggainz?retryWrites=true&w=majority&appName=Cluster0
MONGO_URI=mongodb://localhost:27017/og-gainz

# Legacy compatibility (temporary)
# MONGODB_URI=mongodb://localhost:27017/og-gainz

# Client
# NOTE: Client/vite.config.ts is configured to run dev server on port 8080.
CLIENT_ORIGIN=http://localhost:8080

# Legacy compatibility (temporary)
# CLIENT_URL=http://localhost:8080

# JWT
JWT_SECRET=dev-secret
JWT_EXPIRES_IN=7d

# Cloudinary (Phase 3)
# Prefer setting these three vars. CLOUDINARY_URL is not used by the current server config.

# Use "disabled" as a safe placeholder; upload endpoints will return 503 until configured.
CLOUDINARY_CLOUD_NAME=disabled
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Google OAuth (Phase 2B uses ID-token flow only)
# Required:
GOOGLE_CLIENT_ID=48705627492-k3bqpe2m61i07nhiksqpiv9ecjrp8afu.apps.googleusercontent.com

# Optional (redirect/code flow only - not used in Phase 2B):
GOOGLE_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Email
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=

# Payment (Stripe)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Delivery Configuration
MAX_DELIVERY_DISTANCE_KM=50
BASE_DELIVERY_FEE=5
DELIVERY_FEE_PER_KM=0.5
